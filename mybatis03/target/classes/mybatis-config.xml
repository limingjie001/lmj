<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE configuration
        PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-config.dtd">
<configuration>

    <settings>
<!--        驼峰转换-->
        <setting name="mapUnderscoreToCamelCase" value="true"/>
<!--        开启日志-->
        <setting name="logImpl" value="STDOUT_LOGGING"/>
<!--            开启二级缓存-->
        <setting name="cacheEnabled" value="true"/>
    </settings>
    <!--        别名映射-->
    <typeAliases>

<!--        1、<typeAlias type="com.mybatis.entry.Blog" alias="Blog"/>-->

<!--        2、每一个在包 com.mybatis.pojo 中的 Java Bean，在没有注解的情况下，-->
<!--        会使用 Bean 的首字母小写的非限定类名来作为它的别名。-->
        <package name="com.mybatis.entry"/>

    </typeAliases>

    <environments default="development">
<!--        可以写多个环境，但是每次只能生效一个-->
        <environment id="development">
            <transactionManager type="JDBC"/>
            <dataSource type="POOLED">
                <property name="driver" value="com.mysql.jdbc.Driver"/>
                <property name="url" value="jdbc:mysql://localhost:3306/test?useSSL=true&amp;useUnicode=true&amp;characterEncoding=UTF-8"/>
                <property name="username" value="root"/>
                <property name="password" value="root"/>
            </dataSource>
        </environment>

    </environments>
    <!--每注册一个mapper就写一个-->

<!--    绑定接口-->
    <mappers>

        <!-- 使用相对于类路径的资源引用 -->
<!--        <mapper resource="com/mybatis/mapper/BlogMapper.xml"/>-->

<!--        以下两种方式需要配置文件名称和接口名称一致，并且位于同一目录下-->
<!--       1、 使用映射器接口实现类的完全限定类名-->
<!--        <mapper class="com.mybatis.mapper.BlogMapper"/>-->
<!--       2、将包内的映射器接口实现全部注册为映射器-->
           <package name="com.mybatis.mapper"/>

    </mappers>


</configuration>